let student ={};
student.name="Sagar";
student["age"]=20;

console.log(student.name);
console.log(student[age]);


//Object.keys(obj-name)  -> prints an array of all the keys 
// Object.values(obj-name) -> prints an array of all the values 
// Object.freeze(obj-name) ->now rud operation will not work and obj will be remian unchanged 
